on:
  workflow_dispatch:
jobs:
  pass:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl --proto '=https' --tlsv1.2 -sSf -L https://install.determinate.systems/nix/pr/1639 | sh -x -s -- install --no-confirm
